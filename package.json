{
  "name": "food-delivery-fullstack",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../admin && npm install --include=dev && npm run build && cd ../backend && npm install",
    "start": "node backend/server.js"
  }
}
